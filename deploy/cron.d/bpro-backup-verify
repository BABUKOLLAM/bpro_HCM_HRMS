# Example cron entry for a nightly backup-restore rehearsal.
# Adjust the repo path, database name, and log path for the actual host.
0 4 * * * root cd /root/bpro-hrms-hcm && ./scripts/verify_latest_backup.sh acme_hrms_prod ./backups acme_hrms_verify >> /var/log/bpro-backup-verify.log 2>&1
